baseURL = "https://www.zekeriyaay.com/"
languageCode = 'en-us'
title = "Zekeriya AY"
theme = 'hugo-theme-shell'

googleAnalytics = "G-7LLJDJ25PQ"

# author = "Yukuro"
# prevent build failures when using Hugo's Instagram shortcode due to deprecated Instagram API.
# See https://github.com/gohugoio/hugo/issues/7228#issuecomment-714490456
ignoreErrors = ["error-remote-getjson"]

[Params]
    # Additions start here
    [Params.Navbar]
    use = false

    [Params.Extensions]
    use = true
    ext = ".txt" # extension to add after single page
    # Additions stop here

    # Note: This is for the meta description, which is different from the "description" displayed in the terminal.
    description = "Zekeriya AY's Portfolio! Cybersecurity Analyst, SOC, Blue Team member!"
    [Params.Terminal]
    # Note: color scheme
    # Note: You can choose between
    # Note: hugo-theme-shell original: ["shell-powershell", "shell-ubuntu", "shell-retro"]
    # Note: gogh theme: https://mayccoll.github.io/Gogh/
    scheme = "Molokai"
    # Molokai, Brogrammer, Seti, Atom
    
    # Note: in terminal
    # [userName]@[pcName]:~/$ cd [workDir]
    # [userName]@[pcName]:~/[workDir]$ cat [profile]
    #
    # [description]
    #
    # Note: if you set Params.Tree > use = true
    # [userName]@[pcName]:~/[workDir]$ tree ./[folderName]/
    # ./[folderName]/
    # ...
    # Note: result of the tree command
    userName = "z3k"
    pcName = "z3kbuntu"
    workDir = "secret"
    profile = "whois.txt"

    # Note: speed at which text is displayed on the terminal
    # Note: if set to 0, typing animation will be disabled
    # Note:
    # Note: if you want to enable Mathjax, you need to set it to 0
    # Note: and set "math: true" at front matter in your Markdown file
    ps1Delay = 0      # prompt speed : [userName]@[pcName]:~/$ , [userName]@[pcName]:~/[workDir]$
    stdoutDelay = 1   # stdout speed : [description] , files in Params.Tree
    commandDelay = 30 # command speed : cd [workDir] , cat [profile] , tree ./[folderName]/

    # Note: speed at which text is displayed on the activity pages
    # Note: if set to 0, typing animation will be disabled
    # Note: 
    # Note: if you want to enable Mathjax, you need to set it to 0
    # Note: and set "math: true" at front matter in your Markdown file
    titleDelay = 0   # title speed : "title" in front matter
    contentDelay = 0 # content speed : content in .md file

    description = "description.md"
    # Note: If you want to use a Markdown file, you can use the following
    # description = "/description.md"
    # Note: and put the description.md in /content/description.md

    [Params.Tree]
        useContact = true
        folderNameContact = "contact"
        # Note: ["ACTIVITY", "URL or PATH TO YOUR MARKDOWN FILE"]
        filesContact = [
            [
                "📨email",
                "mailto:zekeriya@zekeriyaay.com",
            ],
            [
                "linkedin",
                "https://linkedin.com/in/ZekeriyaAY",
            ],
            [
                "github",
                "https://github.com/ZekeriyaAY",
            ],
            [
                "blog",
                "https://zekeriyaay.medium.com/",
            ],
            [
                "🐦twitter",
                "https://twitter.com/zekeriyaaycom",
            ],
            [
                "youtube",
                "https://youtube.com/channel/UCcg8zjG1kt-6sRfb4ajHWXQ?sub_confirmation=1",
            ],
        ]

        useBlog = true  # must edit /partials/typeIndex.html  
        folderNameBlog = "blog"
        filesBlog = [
            [
                "🛰️ Arduino Nano & nRF24L01 ile Walkie-Talkie",
                "https://zekeriyaay.medium.com/arduino-walkie-talkie-10ae6113e58e",
            ],
        ]

        useProject = true   # must edit /partials/typeIndex.html  
        folderNameProject = "project"
        filesProject = [
            [
                "pro1",
                "https://github.com/ZekeriyaAY",
            ],
            [
                "pro2",
                "https://linkedin.com/in/ZekeriyaAY",
            ],
        ]

[module]
    [module.hugoVersion]
        extended = true
        min = "0.85.0"
